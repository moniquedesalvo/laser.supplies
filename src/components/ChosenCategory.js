import React, { Component } from 'react';
import materials from '../manualScrape';

// ChosenCategory should display cards that have certain props selected
// Should show top bar where selected categories can be 'x'ed out ....or should this be a separate component?


class ChosenCategory extends Component {

render() {
    return (
      <div>

      </div>
    );
  }
}

export default ChosenCategory;